import {TonConnectButton} from "@tonconnect/ui-react";

export const Header = () => {
    return (
        <header>
            <TonConnectButton style={{ marginLeft: 'auto' }} />
        </header>
    );
};
